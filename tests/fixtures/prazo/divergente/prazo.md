# Stale doc — dublê de documentação desatualizada

A tabela abaixo descreve uma política que não é mais a do `.yaml` ao lado: a
precedência mudou no arquivo e ninguém regravou o bloco. É exatamente o estado
que o caso tem que pegar.

<!-- tabela gerada de .ai/politicas/prazo.yaml — não edite à mão -->

| Origem | Precedência dos campos de prazo | Sem nenhum preenchido |
|---|---|---|
| `unica` | 1. `Campo que já não vale` | `idade_desde_criacao` |

A origem sai do campo `Campo que diz a origem`; vazio, vale `unica`.
Fallback `idade_desde_criacao`: idade, não prazo.
Corrobora, e nunca decide: `Campo numérico de corroboração`.

<!-- fim da tabela gerada -->
